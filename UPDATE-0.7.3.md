# 능력전선 0.7.3-beta.1

## 바뀐 점

- 훈련 모드 요원 선택 화면에 `← 모드 선택`을 추가했습니다.
- 훈련 중에는 `Esc`를 누른 뒤 `메인 화면`으로 즉시 나갈 수 있습니다.
- 멀티플레이 초대를 서버 주소와 방 코드 대신 `초대 코드` 하나로 통합했습니다.

## 친구와 함께 하는 가장 쉬운 방법

### 같은 Wi-Fi 또는 같은 집

1. 방장이 앱을 열고 `사용자설정 모드` 또는 `던전 모드`에서 방을 만듭니다.
2. 방 대기실의 `초대 코드 복사`를 누른 뒤 친구에게 메시지로 보냅니다.
3. 친구는 앱 첫 화면에서 `초대 코드로 참가`를 누르고, 이름을 적은 뒤 코드를 붙여넣습니다.

### 서로 다른 집

1. 방장은 앱 메인 화면에서 `다른 집 친구와 플레이`를 누릅니다.
2. 인터넷 방이 열리면 사용자설정 또는 협동 던전 방을 만듭니다.
3. 대기실의 `초대 코드 복사`를 친구에게 보냅니다.
4. 친구는 `초대 코드로 참가`에 그 코드만 붙여넣습니다.

방장이 앱을 종료하면 방도 닫힙니다. 두 사람 모두 같은 최신 버전을 설치해야 합니다.
