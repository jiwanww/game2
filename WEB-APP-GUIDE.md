# 능력전선 웹 앱 배포

GitHub Pages용 설정은 `.github/workflows/pages.yml`에 들어 있다.

1. 이 업데이트를 `main` 브랜치에 올린다.
2. GitHub 저장소의 **Settings > Pages**를 열고, Source를 **GitHub Actions**로 선택한다.
3. **Actions** 탭의 `Deploy Web App`이 완료되면 저장소의 Pages 주소에서 게임을 실행한다.
4. iPhone Safari에서 주소를 열고 공유 버튼의 **홈 화면에 추가**를 선택한다.

웹 앱에서는 훈련과 싱글 던전이 바로 실행된다. 친구와 실시간 사용자설정 대전을 하려면 별도의 HTTPS 게임 서버 주소를 게임 안에서 연결해야 한다.
